environment variables stored in unix--->
    /etc/environment

to become root user--->
    sudo -i

switch from root user to USER --->
    sudo su <username>

view hidden file--->
    ls -ltrah

command to install via link ---->
    wget <link>

Untar a file -->
    tar -xvf <filename>
